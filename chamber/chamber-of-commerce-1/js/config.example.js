// This is an example configuration file for API keys and other settings.
// Copy this file and rename it to config.js, then fill in your actual values.

const config = {
    openWeatherMapApiKey: 'YOUR_API_KEY_HERE', // Replace with your OpenWeatherMap API key
    chamberLocation: 'YOUR_LOCATION_HERE', // Replace with the location for weather data
};

export default config;